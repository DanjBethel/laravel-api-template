{
  "private": true,
  "scripts": {
    "deploy": "composer pint && composer stan && git add -A && echo 'Enter commit message' && read message && git commit -m \"$message\" && git push",
    "deploy:staging": "git checkout staging && git merge dev && git push && git checkout dev",
    "deploy:prod": "git checkout main && git merge staging && git push && git checkout dev"
  },
  "devDependencies": {
  },
  "dependencies": {
  },
  "prettier": {
  }
}
